<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- 顶部返回 + 标题 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <android.support.v7.widget.AppCompatImageButton
            android:id="@+id/button_back"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@android:color/transparent"
            android:padding="6dp"
            app:srcCompat="@drawable/ic_arrow_back_line"
            android:tint="#222222"
            android:contentDescription="back" />

        <TextView
            android:id="@+id/address_header"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/title_address_management"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#222222"
            android:paddingBottom="8dp" />
    </LinearLayout>

    <TextView
        android:id="@+id/address_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/address_subtitle"
        android:textColor="#666666"
        android:textSize="14sp"
        android:paddingTop="4dp" />

    <!-- 空列表提示 -->
    <TextView
        android:id="@+id/empty_addresses"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:padding="12dp"
        android:text="@string/address_empty"
        android:textColor="#666666"
        android:visibility="gone" />

    <!-- 地址列表 -->
    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_addresses"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:clipToPadding="false"
        android:paddingBottom="12dp" />

    <!-- 新增地址按钮：使用 TextView 模拟 Button，并使用硬编码文本绕过资源加载错误 -->
    <TextView
        android:id="@+id/button_add_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:background="#FF4081"
        android:text="新增收货地址"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:padding="14dp"
        android:gravity="center"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>
